**/.DS_Store
/workspace/
# Regression tests ARE version-controlled (CI + reviewers need them; they double
# as executable docs of expected behavior). Only their bytecode is ignored.
/test/__pycache__/
.kiro/settings/
__pycache__/
*.pyc

# Backup tool credentials — never commit
backup/config

# Conversion output (written to the user's working dir; ignore if run inside the repo)
cloudflare-to-aws-waf/
cloudflare-to-aws-cdn/

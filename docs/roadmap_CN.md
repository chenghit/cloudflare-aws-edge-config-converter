[English](./roadmap.md)

# 路线图

CDN 完整流程（9 阶段 pipeline）已全部实现并可用。

当前状态：

| 流程 | 阶段数 | 状态 |
|------|--------|------|
| WAF 转换 | 3（分析 → 校验 → 生成 Terraform） | ✅ 可用 |
| CDN 转换 | 9（DNS 解析 → 用户输入 → 规则处理 → IR 校验 → 合并去重 → 最终校验 → 共享策略 → 域名 TF → JS 校验） | ✅ 可用 |

后续计划：

- 性能优化和用户体验改进
- 更多 Cloudflare 规则类型的支持
- 端到端测试覆盖
